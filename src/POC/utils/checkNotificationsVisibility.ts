import { StripesType } from '@folio/stripes-types/core';

// TODO: implement the actual logic for checking whether to show "Bell" icon for notifications center
export const checkNotificationsVisibility = (stripes: StripesType) => {
  return true;
};
